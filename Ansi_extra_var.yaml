---
- name: create file
  hosts: server2.example.com
  become: yes
  become_method: sudo
  become_user: root    
  tasks:
  - name: Creating an empty file
    file:
      path: "/tmp/{{ deploy }}.txt"
      state: touch
      mode: '0644'
